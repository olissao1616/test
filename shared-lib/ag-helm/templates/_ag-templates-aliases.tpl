{{/*
This file is intentionally left without template definitions.

This chart exposes only the singular public API:
- ag-template.<resource>

The deprecated plural aliases (ag-templates.<resource>) were removed to avoid maintaining duplicate entry points.
*/}}
